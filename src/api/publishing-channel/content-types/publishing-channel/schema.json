{
  "kind": "collectionType",
  "collectionName": "publishing-channels",
  "info": {
    "singularName": "publishing-channel",
    "pluralName": "publishing-channels",
    "displayName": "Publishing Channel"
  },
  "options": { "draftAndPublish": false },
  "attributes": {
    "name": { "type": "string", "required": true, "unique": true },
    "slug": { "type": "uid", "targetField": "name", "required": true }
  }
}